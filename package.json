{
	"name": "parseapi-mcp",
	"version": "0.3.0",
	"mcpName": "com.parseapi/mcp",
	"description": "Official parseAPI MCP server. Look up places, addresses, company numbers, email, phone, weather, dates and more.",
	"keywords": [
		"parseapi",
		"mcp",
		"model context protocol",
		"agents",
		"geolocation",
		"email validation",
		"weather"
	],
	"license": "MIT",
	"author": "parseAPI <hello@parseapi.com> (https://parseapi.com)",
	"homepage": "https://parseapi.com",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/parseapi/mcp.git"
	},
	"bugs": {
		"url": "https://github.com/parseapi/mcp/issues"
	},
	"type": "module",
	"bin": {
		"parseapi-mcp": "./dist/stdio.js"
	},
	"exports": {
		".": "./dist/registry.js",
		"./http": "./dist/http.js"
	},
	"files": [
		"dist"
	],
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"build": "tsup",
		"typecheck": "tsc --noEmit",
		"smoke": "npm run build && node smoke/smoke.mjs",
		"serve": "npm run build && node dist/http.js",
		"test": "npm run build && node --test test/*.test.mjs",
		"prepublishOnly": "npm run typecheck && npm test"
	},
	"dependencies": {
		"@modelcontextprotocol/server": "^2.0.0",
		"@modelcontextprotocol/node": "^2.0.0",
		"@parseapi/sdk": "^0.3.0",
		"zod": "^4.0.0"
	},
	"devDependencies": {
		"@types/node": "^22.10.0",
		"tsup": "^8.3.5",
		"typescript": "^5.7.0"
	}
}
